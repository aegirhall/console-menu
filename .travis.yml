language: python

os:
  - linux

sudo: false

cache: pip

python:
  - "3.6"

install:
  - pip install -rv requirements.txt
  - pip install -v pep8
  - pip install -v pytest

script:
  #  - 'python setup.py test --tox-args="-e$TOX_ENV"'
  - 'py.test'
  - 'pep8 --max-line-length=120'

#env:
#  #- TOX_ENV=py27
#  #- TOX_ENV=py34
#  #- TOX_ENV=py35
#  - TOX_ENV=py36
#  #- TOX_ENV=pypy
#  #- TOX_ENV=pypy3
#  - TOX_ENV=docs
#  - TOX_ENV=pep8

matrix:
  include:
    - python: "nightly"
      #env: TOX_ENV=py36
  allow_failures:
    - python: "nightly"
    #- python: "2.6"
  fast_finish: true


